# lgdx_frontend 프로젝트 개요

## 문서 목적

이 문서는 설치 방법이나 앱 실행 가이드를 위한 문서가 아니라, 실무진이 이 저장소를 포트폴리오 관점에서 이해할 수 있도록 정리한 개요 문서입니다.

`lgdx_frontend`와 `lgdx_backend`는 하나의 프로젝트를 프론트엔드와 백엔드로 나눠 관리한 구조이며, 이 저장소는 그중 사용자 경험을 담당하는 영역입니다.

## 프로젝트 한 줄 요약

매뉴얼 기반 답변, 실시간 상호작용, 생성형 미디어 결과를 실제 사용자 화면 안에서 소비할 수 있도록 구성한 Flutter 애플리케이션입니다.

이 저장소는 단순 UI 샘플이 아니라, 백엔드의 검색/생성 결과를 사용자가 체감하는 인터페이스로 바꾸는 역할을 합니다.

## 내가 맡은 역할

이 프로젝트에서는 백엔드와 별도로 분리된 프론트엔드 레포를 통해, 사용자 경험이 실제 서비스 시나리오처럼 동작하도록 화면 흐름을 구성하는 역할을 맡았습니다.

이 저장소 기준으로 제가 맡은 역할은 아래와 같습니다.

- Flutter 기반 주요 화면 구조 설계
- 채팅, 라이브, 영상 재생 등 핵심 사용자 흐름 연결
- Firebase/Firestore 상태를 앱 화면과 연동
- 백엔드 API 호출 결과를 사용자 인터랙션으로 변환
- 단순 화면 구현을 넘어서, AI 결과가 실제 UX로 소비되도록 앱 경험 구성

포트폴리오 관점에서 보면, 이 저장소는 제가 "Flutter 화면을 그렸다"보다 "서버에서 만들어진 결과를 사용자 경험으로 연결할 수 있다"는 점을 보여주는 영역입니다.

## 어떤 문제를 풀고자 했는가

백엔드에서 검색과 생성이 잘 되더라도, 사용자가 그것을 자연스럽게 사용할 수 없으면 서비스로 완성되지 않습니다.

이 프로젝트는 아래 질문에 대한 프론트엔드 해답을 담고 있습니다.

- 제품 문제 해결 경험을 앱 화면에서 어떻게 전달할 것인가
- 텍스트 답변, 라이브 화면, 영상 결과를 하나의 흐름으로 어떻게 연결할 것인가
- 생성된 결과를 사용자가 다시 상호작용 가능한 형태로 어떻게 소비하게 할 것인가

즉, 이 저장소는 "예쁜 화면"보다 "AI 기능이 실제 사용자 경험으로 전환되는 방식"을 보여주는 프론트엔드입니다.

## 이 저장소가 맡은 책임

`lgdx_frontend`는 전체 서비스에서 아래 책임을 가집니다.

- 사용자가 직접 마주하는 앱 화면 구성
- 채팅 기반 문제 해결 흐름 제공
- 라이브/카메라/비디오 재생 경험 구성
- Firebase 기반 상태 반영
- 백엔드 API 호출 결과를 사용자 인터랙션으로 연결

실무 관점에서는 단순한 화면 구현보다, 서버 결과를 앱 상태와 UX로 변환하는 presentation layer이자 interaction layer로 보는 것이 맞습니다.

## 왜 이 저장소가 별도 레포인지

이 프로젝트는 사용자 경험과 서버 파이프라인의 변경 포인트가 다르다고 판단해 저장소를 분리했습니다.

- 프론트엔드는 화면 흐름, 컴포넌트, 사용자 반응 속도에 집중
- 백엔드는 검색, 생성, 저장, 외부 연동에 집중

이 분리를 통해 각 역할을 명확히 하고, 하나의 프로젝트를 더 읽기 쉬운 단위로 나눴습니다.

## 이 저장소에서 보여주고 싶은 역량

### 1. AI 결과를 UX로 번역하는 능력

이 저장소는 단순히 API 응답을 출력하는 앱이 아닙니다.

- 채팅 응답이 어디에 보이는지
- 라이브 상호작용이 어디서 이어지는지
- 생성된 영상이 어떤 화면에서 재생되는지

이런 연결을 설계했다는 점이 핵심입니다.

### 2. 상태를 화면 경험으로 다루는 능력

사용자 지원형 앱은 결과가 한 번에 끝나지 않습니다.

- 요청 전송
- 응답 대기
- Firestore 상태 반영
- 영상 생성 완료 후 재생 화면 전환

이 저장소는 이런 비동기 흐름을 화면 경험으로 풀어낸 흔적이 있는 프로젝트입니다.

### 3. 멀티모달 사용자 경험에 대한 시도

코드 구조상 텍스트 채팅만이 아니라 아래 경험을 함께 다룹니다.

- 채팅
- 라이브 화면
- 카메라 입력
- 비디오 플레이어

즉, 이 저장소는 단순 앱 UI가 아니라, 복수의 입력/출력 수단을 갖는 사용자 지원 경험을 설계한 결과물로 볼 수 있습니다.

### 4. 서비스 시나리오를 화면 단위로 분해하는 능력

`lib/screens/` 구조를 보면 홈, 채팅, 라이브, 재생 화면이 분리돼 있습니다.

이건 페이지를 많이 만든다는 의미보다, 사용자 여정을 화면 단위로 나누어 구성하려는 습관을 보여줍니다.

## 구조를 어떻게 보면 좋은가

### `vision_app/lib/screens/`

사용자 여정이 가장 잘 보이는 영역입니다.

채팅, 라이브, 홈, 비디오 재생 등 핵심 화면이 어떤 식으로 분리돼 있는지 보면 앱이 어떤 시나리오를 전제로 만들어졌는지 확인할 수 있습니다.

### `vision_app/lib/services/`

앱이 외부와 연결되는 경계입니다.

API 호출, 카메라 서비스 같은 로직이 이곳에 있어 화면 코드와 연동 코드의 경계를 어떻게 나눴는지 볼 수 있습니다.

### `vision_app/lib/models/`

채팅 메시지 등 화면에 직접 연결되는 데이터 모델이 위치한 영역입니다.

### `vision_app/lib/config/`

백엔드 연동 지점과 실행 환경을 읽을 수 있는 영역입니다.

## 실무진이 이 저장소에서 보면 좋은 포인트

- 단순한 Flutter 샘플이 아니라는 점
- 화면이 서버 기능과 명확히 연결되어 있다는 점
- 채팅, 라이브, 영상 재생까지 이어지는 시나리오가 있다는 점
- 상태 변화가 사용자 경험으로 어떻게 반영되는지 읽을 수 있다는 점

즉, 이 저장소는 "Flutter를 쓸 줄 안다"보다 "서버 중심 기능을 실제 앱 경험으로 풀어낼 수 있다"는 점을 보여주는 포트폴리오입니다.

## 함께 봐야 하는 저장소

- `lgdx_backend`

백엔드 저장소를 함께 보면, 이 프론트엔드가 소비하는 답변 생성, 매뉴얼 검색, 영상 생성 흐름이 어떤 구조로 만들어졌는지 확인할 수 있습니다.

두 저장소를 함께 보셨을 때 제가 보여드리고 싶은 것은 화면 구현 자체보다, 하나의 서비스 경험을 프론트와 백엔드로 나누어 설계하고 연결한 방식입니다.
