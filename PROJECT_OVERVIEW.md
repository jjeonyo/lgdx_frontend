# lgdx_frontend 프로젝트 개요

## 한 줄 소개

`lgdx_frontend`는 `lgdx_backend`와 함께 동작하는 하나의 제품형 프로젝트에서 사용자 경험과 화면 흐름을 담당하는 Flutter 저장소입니다.

이 프로젝트는 채팅, 라이브 카메라, 영상 재생, Firebase 연동을 통해 백엔드의 검색/생성 결과를 실제 사용자 인터페이스로 전달하는 것을 목표로 합니다.

## 이 저장소의 역할

이 저장소는 전체 프로젝트에서 아래 역할을 맡습니다.

- 사용자에게 보이는 Flutter 앱 화면 구성
- 채팅 기반 상호작용 흐름 제공
- 라이브 화면, 카메라, 비디오 재생 경험 제공
- Firebase와 연동해 메시지/상태를 반영
- 백엔드 API 호출 결과를 사용자 경험으로 연결

즉, `lgdx_backend`가 서버와 생성 파이프라인을 담당한다면 `lgdx_frontend`는 그 결과를 사용자에게 자연스럽게 보여주는 앱 레이어입니다.

## 전체 프로젝트 관점에서의 구조

전체 프로젝트는 크게 두 저장소로 나뉩니다.

- `lgdx_frontend`
  사용자 화면, 인터랙션, 앱 내 이동 흐름
- `lgdx_backend`
  검색, 답변 생성, 미디어 생성, 저장 및 외부 서비스 연동

두 저장소는 분리돼 있지만, 실제로는 하나의 사용자 지원 서비스를 구성하는 프론트와 백엔드입니다.

## 사용자 기준 핵심 흐름

### 1. 채팅 기반 지원 흐름

- 사용자가 앱에서 질문을 입력
- 프론트가 백엔드 API에 요청 전달
- 백엔드 응답을 채팅 UI에 표시
- Firebase/Firestore에 기록된 메시지를 화면 상태와 연결

### 2. 라이브/카메라 기반 상호작용

- 사용자가 라이브 화면 또는 카메라 기능 사용
- 프론트가 기기 기능과 앱 상태를 제어
- 필요 시 서버와 연계해 상황 기반 응답 또는 미디어 흐름을 이어감

### 3. 영상 결과 소비

- 백엔드가 생성한 영상 또는 상태 정보를 앱에서 수신
- 비디오 플레이어 화면 또는 오버레이 화면으로 연결
- 사용자는 생성 결과를 앱 안에서 바로 확인

## 디렉터리 기준으로 보면

### `vision_app/`

이 저장소의 핵심 앱 코드가 들어 있는 Flutter 프로젝트입니다.

주요 구성은 아래와 같습니다.

- `lib/screens/`
  채팅, 라이브, 홈, 영상 재생 등 화면 단위 코드
- `lib/services/`
  API 호출, 카메라 서비스 등 앱 외부와 연결되는 로직
- `lib/models/`
  채팅 메시지 등 앱 상태 모델
- `lib/config/`
  서버 주소 등 환경 설정
- `assets/`
  화면에 필요한 이미지와 리소스

## 화면 흐름 관점에서의 특징

코드 구조상 아래 화면이 중심입니다.

- 채팅 화면
- 고객 지원 또는 홈 화면
- 라이브 화면
- 영상 재생 화면
- 업적/카드형 UI 화면

즉, 이 저장소는 단순 샘플 앱이 아니라, 사용자 지원 경험을 하나의 앱 시나리오로 엮으려는 방향이 분명한 프론트엔드 프로젝트입니다.

## 기술 스택

코드 기준으로 확인되는 핵심 스택은 아래와 같습니다.

- Flutter
- Dart
- Firebase Core
- Cloud Firestore
- HTTP API 연동
- Camera
- WebRTC / 오디오 및 비디오 재생 관련 패키지

## 이 저장소를 읽을 때의 권장 관점

이 저장소는 화면만 모아둔 앱이 아니라, 백엔드에서 생성한 정보와 상태를 사용자 행동으로 이어주는 인터페이스 계층입니다.

그래서 코드를 볼 때는 개별 위젯보다 아래 흐름을 같이 보는 것이 좋습니다.

- 어떤 화면에서 어떤 API를 호출하는지
- Firestore 상태가 어느 화면에 반영되는지
- 생성된 비디오나 채팅 결과가 어떤 UX로 소비되는지

## 함께 봐야 하는 저장소

- `lgdx_backend`

백엔드 저장소에는 RAG 처리, 매뉴얼 검색, 생성형 미디어 파이프라인, 데이터 저장 흐름이 들어 있습니다.

두 저장소는 서로 독립적인 별도 프로젝트가 아니라, 하나의 제품형 프로젝트를 프론트와 백엔드로 나눠 관리하는 구조입니다.

